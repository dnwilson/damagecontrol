<%= bootstrap_form_for(@post, :validate => true, :html => {:class => 'post-form', :multipart => true, :autocomplete => :off}) do |f| %>
	<%= render 'layouts/messages', object: f.object %>

	<%= f.text_field :title %>
	<%= f.text_field :body %>
	<%= f.submit "Create Post" %>

<% end %>